Description: fix generated sample to fit debian
Author: KURASHIKI Satoru <lurdan@gmail.com>
Bug-Debian: http://bugs.debian.org/716662
Origin: vendor

--- ruby-serverspec-0.6.28.orig/lib/serverspec/setup.rb
+++ ruby-serverspec-0.6.28/lib/serverspec/setup.rb
@@ -48,11 +48,11 @@ EOF
       content = <<-EOF
 require 'spec_helper'
 
-describe package('httpd') do
+describe package('apache2') do
   it { should be_installed }
 end
 
-describe service('httpd') do
+describe service('apache2') do
   it { should be_enabled   }
   it { should be_running   }
 end
@@ -61,7 +61,7 @@ describe port(80) do
   it { should be_listening }
 end
 
-describe file('/etc/httpd/conf/httpd.conf') do
+describe file('/etc/apache2/sites-enabled/000-default') do
   it { should be_file }
   it { should contain "ServerName #{@hostname}" }
 end
